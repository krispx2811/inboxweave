-- After running `supabase db reset`, create a user via:
--   Supabase Studio → Authentication → Add user
-- then paste that user's UUID below and run this file with `psql`.
-- (Or use the /admin/bootstrap route once signed in for the first time.)

-- insert into public.platform_admins(user_id) values ('PASTE-USER-UUID-HERE');
